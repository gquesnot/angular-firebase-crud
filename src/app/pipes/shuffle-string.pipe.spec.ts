import { ShuffleStringPipe } from './shuffle-string.pipe';

describe('ShuffleStringPipe', () => {
  it('create an instance', () => {
    const pipe = new ShuffleStringPipe();
    expect(pipe).toBeTruthy();
  });
});
